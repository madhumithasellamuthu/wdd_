<?php
// Set a test cookie
$cookie_name = "TestCookie";
$cookie_value = "HelloWorld";

// Set cookie to expire in 1 hour (3600 seconds)
setcookie($cookie_name, $cookie_value, time() + 3600, "/");

// Check if cookie is set
if(isset($_COOKIE[$cookie_name])){
    echo "Cookie '{$cookie_name}' is already set. Value: " . $_COOKIE[$cookie_name];
} else {
    echo "Cookie '{$cookie_name}' has been created! Refresh the page to see it.";
}
?>

OUTPUT:

Cookie 'TestCookie' has been created! Refresh the page to see it.
