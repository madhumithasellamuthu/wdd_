<?php
// Function to check leap year
function isLeapYear($year) {
    if (($year % 400 == 0) || ($year % 4 == 0 && $year % 100 != 0)) {
        return true;
    } else {
        return false;
    }
}

// Example usage
$year = 2024; // Change this to test different years

if (isLeapYear($year)) {
    echo "$year is a Leap Year.";
} else {
    echo "$year is NOT a Leap Year.";
}
?>


output:

2024 is a Leap Year.
