<?php
$a = 5;
$b = "5";
$c = 5;

// Check if $a equals $b (loose comparison)
echo "\$a == \$b: ";
var_dump($a == $b);  // true because values are equal (type not checked)

// Check if $a is identical to $b (strict comparison)
echo "\$a === \$b: ";
var_dump($a === $b); // false because types are different

// Check if $a equals $c (loose comparison)
echo "\$a == \$c: ";
var_dump($a == $c);  // true

// Check if $a is identical to $c (strict comparison)
echo "\$a === \$c: ";
var_dump($a === $c); // true

// Compare all three variables together
echo "All equal (loose): ";
var_dump($a == $b && $b == $c);

echo "All identical (strict): ";
var_dump($a === $b && $b === $c);
?>
OUTPUT:

$a == $b: bool(true)
$a === $b: bool(false)
$a == $c: bool(true)
$a === $c: bool(true)
All equal (loose): bool(true)
All identical (strict): bool(false)
